<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.spring.bean.sang.dao.ArticleDao">
    <select id="getArticleById" resultType="com.study.spring.bean.sang.entity.Article">
        select * from article family where id = #{id}
    </select>
    <select id="getFirst10Article" resultType="com.study.spring.bean.sang.entity.Article">
        select family.*,c.name as category from article family,category c WHERE family.categoryId=c.id limit 10
    </select>
    <resultMap id="categoryType" type="com.study.spring.bean.sang.entity.Category">
        <result property="displayName" column="display_name"/>
    </resultMap>
    <select id="getCategories" resultMap="categoryType">
        SELECT * FROM category
    </select>
    <insert id="writeBlog" parameterType="com.study.spring.bean.sang.entity.Article">
        INSERT INTO article(title,content,categoryId,summary,date) VALUES (#{title},#{content},#{categoryId},#{summary},#{date})
    </insert>
    <select id="getCategoryIdByName" parameterType="string" resultType="long">
        SELECT id FROM category WHERE name=#{name}
    </select>
    <delete id="deleteArticleById" parameterType="long">
        DELETE FROM article WHERE id=#{id}
    </delete>
    <update id="updateArticleById" parameterType="com.study.spring.bean.sang.entity.Article">
        UPDATE article set title=#{title},content=#{content},summary=#{summary},date=#{date} WHERE id=#{id}
    </update>
    <select id="getCategoryById" parameterType="long" resultMap="categoryType">
        SELECT * FROM category WHERE id=#{id}
    </select>
    <select id="getArticlesByCategoryName" parameterType="long" resultType="com.study.spring.bean.sang.entity.Article">
        SELECT family.*,c.name as category FROM article family,category c WHERE family.categoryId=c.id AND categoryId=#{categoryId}
    </select>
</mapper>